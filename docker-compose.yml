services:
  shakti-dashboard:
    build: .
    restart: unless-stopped
    container_name: shakti-dashboard
    ports:
      - "5173:5173"
    env_file:
      - .env
    networks:
      - shakti-network

networks:
  shakti-network:
    driver: bridge