# API Payload Samples

This directory contains sanitized API response payloads gathered from the backend services during verification checks.

> [!WARNING]
> DO NOT commit raw server payloads that contain secrets, signatures, auth tokens, passwords, or personal keys. Always replace confidential fields with sanitized placeholders (e.g., `"<AUTH_TOKEN>"`).
